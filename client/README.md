# Fish Cart - Client

React frontend for Fish Cart application.

## Setup

1. Install dependencies:
```bash
npm install
```

2. Start development server:
```bash
npm run dev
```

3. Build for production:
```bash
npm run build
```

## Configuration

The API URL is configured in `src/utils/api.js`. Default is `http://localhost:5000/api`.
